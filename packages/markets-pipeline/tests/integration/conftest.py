"""Integration test conftest.

``warehouse`` and ``stage4_seeded_fixtures`` are defined in the parent
``tests/conftest.py`` and are automatically inherited by pytest here.
No additional fixtures are needed for Stage 4 integration tests.
"""
